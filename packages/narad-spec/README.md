# @devkrity/narad-spec

Canonical Narad protocol package: JSON Schema, conformance traces, and the `narad/v1` prose spec.

The TypeScript client (`@devkrity/narad`) and React bindings (`@devkrity/narad-react`) consume these traces. This package has no runtime code.
